import React from 'react';

const SearchBar = (props) => {
    return (
        <header>
            <Logos />
            <SearchBox />
            <UserIcons />
        </header>
    );
}

export default SearchBar;